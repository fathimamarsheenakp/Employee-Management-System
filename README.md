# Employee-Management-System
A Java-based Employee Management System with a JSwing GUI, demonstrating database connectivity with MySQL. The application supports CRUD operations, including fetching all employees, adding new employees, searching, updating, and deleting employee records.
